const btn = document.querySelector(".right-panel-link");

const signup_box = document.querySelector(".right-panel-link");

right-panel-link.onclick = ()=>{
  
    right-panel-link.classList.add("form"); 
    showQuetions(0); 
    queCounter(1); 
    startTimer(15); 
    startTimerLine(0); 
  }

  